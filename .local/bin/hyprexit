#!/usr/bin/env bash

CHOICE=$(echo -e "Yes\nNo" | wofi --dmenu --width 200 --height 100 --prompt "Do you want to log out?")

if [[ "$CHOICE" == "Yes" ]]; then
  hyprctl dispatch exit
else
  echo "No selected"
fi
